version: 2.1

# Define the jobs we want to run for this project
jobs:
  build:
    machine: true
    steps:
        - add_ssh_keys: 
            fingerprints:
                - "c5:a4:0e:6f:39:cc:5d:8e:d4:f0:33:f2:12:c7:dc:67"
        - checkout
        - run: docker login -u $DOCKER_USER -p $DOCKER_PASS
        - run: docker build -t cabrecrieuro/dockerninja:$CIRCLE_SHA1 .
        - run: docker push cabrecrieuro/dockerninja:$CIRCLE_SH1
        - run: ssh -o StrinctHostKeyChecking=no cabrecri@outlock.com "/bin/bash /root/docker/deploy.sh $CIRCLE_SH1
